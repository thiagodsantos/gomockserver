package constants

// Server constants
const (
	ProxyServerPort = ":8080"
)

// HTTP constants
const (
	MethodGet  = "GET"
	MethodPost = "POST"
)

// JSON constants
const (
	JSONExtension   = "json"
	JSONContentType = "application/json"
)
